# 松手 / letgo 设计

**背景：** 同属 [[2026-07-04-infp-90day-plan]] 摩擦点驱动的产品方向，与 [[unstuck]] 是同一"拆解摩擦点"家族，但机制不同——unstuck 针对"情绪卡住导致的任务拖延"，letgo 针对"报复性熬夜"（revenge bedtime procrastination）：知道该睡了，却不想放下手机。

**核心机制：** 报复性熬夜的驱动因素不是手机本身多好玩，而是白天缺乏自主感，晚上熬夜是唯一能抢回来的"我的时间"（Kroese et al. 2014）。但用户在设计过程中明确了不需要单独的心理引导开场/收尾（"自主感命名"/"我选择"确认被拿掉），改为直接提供一套有科学依据的具体睡前仪式动作序列，靠动作本身完成心理效果。

**目标：** 手机端离线小工具，五步引导睡前仪式，无需联网、无需账号，走完生成一行记录方便粘贴进 `raw.md`。

---

## 五步流程

### 步骤1：洗漱

刷牙 3 分钟倒计时 → 洗脚 3 分钟倒计时，两段顺序进行。

**依据：** Kräuchi et al., *Nature* (1999) —— 暖脚促进外周血管扩张，加速核心体温下降，这是入睡的生理信号之一。

### 步骤2：清空大脑

自由文字输入区，把脑子里转的念头写出来，写完点"倒空了"进入下一步。**内容不保存**，写下来本身就是目的。

**依据：** Scullin et al. (2018, Baylor University) —— 睡前把脑子里的念头/待办写下来，比不写的人入睡明显更快（认知卸载）；Zeigarnik 效应——未完成/未记录的念头会持续侵扰注意力，写下来能让大脑停止"排练"这些念头。

**界面文案要求：** 这一步的提示文字要明确告诉用户"写的这些是垃圾、是倒掉的东西，不会被保存"，让用户在动笔前就知道内容不会留存，不用担心措辞、不用有负担——例如提示语可以是"这里写的都是垃圾，写完就倒掉，不会被保存，随便写"。这样能让用户更放心地把真实念头倒出来，而不是又开始斟酌怎么写。

**顺序说明：** 清空大脑排在冥想之前，是因为如果先冥想，脑子里没写下来的念头会持续打断冥想的专注，效果打折——先清空再冥想，冥想时残留的杂念更少。

### 步骤3：冥想

用户可选时长（3 / 5 / 10 / 15 分钟，默认 10 分钟）。视觉呼吸引导（圆圈随呼吸节奏收缩扩张）。可选开启：
- **语音提示**：浏览器自带语音合成（Web Speech API `speechSynthesis`），念"吸气/呼气"，无需网络无需音频文件
- **水流白噪音**：Web Audio API 实时合成的类流水声（噪声经滤波处理），仅在本步骤内播放，进入下一步自动停止

**依据：** 正念冥想降低睡前认知和生理唤醒水平，是失眠认知行为疗法（CBT-I）的标准组成部分。

### 步骤4：感恩三件事

三个小输入框，各写一件今天值得感恩的事。

**依据：** Digdon & Koble (2011, *Applied Psychology: Health and Well-Being*) —— 睡前感恩书写能降低睡前担忧、提升睡眠质量。

### 步骤5：看书（最后一步）

提示："把手机放到客厅，去看书。" 这是手机上的最后一次操作——用户接下来会把手机留在客厅，不再使用。本屏同时生成记录行 + 复制按钮。

**依据：** 睡眠卫生指南推荐的经典睡前活动，不带屏幕，帮助从清醒切换到困倦；卧室不放手机是标准睡眠卫生建议。

---

## 记录格式

走完流程后生成一行，供复制粘贴进 `C:\Users\lucky\ideas\raw.md`：

```
- [YYYY-MM-DD HH:mm] [sleep] 感恩:<事1>；<事2>；<事3>
```

流程是线性的、没有跳过某一步直接到最后一屏的路径，所以"走到记录生成这一屏"本身就意味着五步都完成了——不需要单独记录"完成了哪些步骤"这种总是同一个值的字段。步骤2"清空大脑"的书写内容本身不进入记录（只用于卸载念头，不用于留档）。

---

## 技术与发布

- 单文件 HTML/CSS/JS，无框架、无外部依赖，与 `unstuck-mobile.html` 同一技术路线
- 语音提示和白噪音均为浏览器原生 API 实时生成（`speechSynthesis` / Web Audio API 噪声+滤波），不嵌入任何音频文件，不发起网络请求
- 中英双语切换，右上角 `中/EN` 按钮，实现方式与 unstuck 一致
- 视觉延续 unstuck 的设计系统（卡片/圆点进度/CSS 变量主题结构），主色调换成夜间氛围色（更深的靛蓝/墨蓝），做视觉区分
- localStorage 保存记录（可选，供用户自己查看历史）
- **只发布到 GitHub Pages，不发布 Claude Artifact**（用户明确要求）

## 项目文件夹

`C:\Users\lucky\ideas\projects\letgo\`：
- `design.md`（本文件）
- `plan.md`（实施计划）
- `letgo-mobile.html`（工具源文件）

单任务项目，不再拆 task-N 子文件夹（对比 unstuck 有桌面+手机两个任务才拆分）。

---

## 不做的事（范围边界）

- 不做桌面端 Claude Code skill（睡前场景不会在电脑前用）
- 不嵌入真人录音或音乐文件（文件体积和版权问题，改用程序化合成的语音提示和白噪音）
- 不做云同步、不做账号体系
- 不保留"自主感命名"开场白和"我选择"确认收尾（用户设计阶段明确拿掉，改为直接进入具体仪式动作）
- 步骤2清空大脑的书写内容不保存、不留档
